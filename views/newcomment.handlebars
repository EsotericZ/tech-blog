{{> navbar}}

<div class="card">
  <h1 class="card-header">Add A Comment</h1>
  <h1></h1>
  <form class="card-body new-post-form" id="new-post-form">
    {{!-- <label for="remarksField" class="form-label">Title</label> --}}
    <input type="text" class="form-control" id="remarksField" aria-describedby="emailHelp">
    <button type="submit" class="btn btn-dark" id="addCommentBtn">Submit</button>
  </form>
</div>

{{#each userPosts as |post| }}
      <h2>{{post.title}}</h2>
      <h2>{{post.id}}</h2>
 {{/each}}